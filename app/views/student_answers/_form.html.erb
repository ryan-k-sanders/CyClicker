<%= form_for(@student_answer, :remote => true) do |f| %>
  <% if @student_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_answer.errors.count, "error") %> prohibited this student_answer from being saved:</h2>

      <ul>
      <% @student_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :studentID %><br />
    <%= f.text_field :studentID %>
  </div>
  <div class="field">
    <%= f.label :questionID %><br />
    <%= f.number_field :questionID %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit(:class => 'button') %>
  </div>
<% end %>
