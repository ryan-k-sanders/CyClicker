<%= form_for(@question_answer, :remote => true) do |f| %>
  <% if @question_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_answer.errors.count, "error") %> prohibited this question_answer from being saved:</h2>

      <ul>
      <% @question_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answerText %><br />
    <%= f.text_field :answerText %>
  </div>
    <div class="field">

      <%= f.hidden_field :questionID, :value => params[:id] %>
    </div>
  <div class="actions">
    <%= f.submit(:class => 'button') %>
  </div>
<% end %>
