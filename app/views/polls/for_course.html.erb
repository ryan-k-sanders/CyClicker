<h1>Listing polls</h1>

<table>
  <tr>
    <th>Coursecode</th>
    <th>Name</th>
    <th>Open</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @polls.each do |poll| %>
  <tr>
    <td><%= poll.courseCode %></td>
    <td><%= poll.name %></td>
    <td><%= poll.open %></td>
    <td><%= link_to 'Show', poll %></td>
    <td><%= link_to 'Edit', edit_poll_path(poll) %></td>
    <td><%= link_to 'Destroy', poll, confirm: 'Are you sure?', method: :delete, :remote => true, :id=>'delete' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Poll', new_poll_path %>
